# -------------------- Configuraci�n de conexi�n a MySQL --------------------

spring.devtools.livereload.enabled=true
server.port=8080

# Configuraci�n para MySQL 8
spring.datasource.url=jdbc:mysql://localhost/bingo2?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrival=true

# Usuario de base de datos
spring.datasource.username=root

# Contrase�a para contact de base de datos
spring.datasource.password=tattortega.95

# Clase a usar para conectar con la base de datos
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Dialecto a usar de SQL en este caso MySQL8
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Actualizar la estructura de la base de datos
spring.jpa.hibernate.ddl-auto=update

# -------------------- Mostrar SQL que se est� ejecutando LOG --------------------

# Formatear salida del SQL
spring.jpa.properties.hibernate.format_sql=true

# Hacer que se muestre el SQL ejecutado
logging.level.org.hibernate.SQL=DEBUG

# Hacer que se muestre los valores que se inyectan a la sentencia SQL
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE



